@model IEnumerable<AudioProntuario.Models.Audio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AudioProntuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AudioProntuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAudio }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAudio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAudio })
        </td>
    </tr>
}

</table>
